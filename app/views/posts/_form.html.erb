<%= simple_form_for(post) do |f| %>
    <%= f.input :name, :label => "Titre" %>
    <%= f.input :description %>
    <%= f.input :content, :label => "Contenu" %>

  <%= f.button :submit, "Poster", class: "btn-primary" %>
<% end %>
